<?php
namespace App\Traits;
use DateTime;

trait DateTimeValidate{
    public function validateDate($date, $format = 'Y-m-d H:i:s'): bool
    {
        $d = DateTime::createFromFormat($format, $date);
        return $d && $d->format($format) == $date;
    }
}